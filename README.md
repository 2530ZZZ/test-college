project/

├── config.py            # 测速参数

├── tester.py            # 测速模块

├── proxy_model.py       # 统一数据模型（已有）

├── parsers.py           # 解析器（已有）

├── utils.py             # 工具函数（已有）

├── collector.py         # 搜集模块（已有）

├── main.py              # 入口，调用搜集 + 测速

└── .github/workflows/collect.yml
